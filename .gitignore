node_modules/
dist/
.astro/
.vercel/
.vercel

# Data: the raw ATUS downloads are fetched by the pipeline, never committed.
# (Ticket 02 pinned their URLs + SHA-256s; ticket 03 re-fetches and re-verifies.)
data/raw/
# The validated extract is derived, reproducible with `npm run data`, and ~25 MB.
data/extract/

# Site payloads (public/data, src/gen) are generated by scripts/site/build-payloads.mjs
# from the gitignored extract, so they ARE committed — Vercel can't rebuild them.
src/data/

# Local scratch — never ship
.scratch/

# Ticket-04 spike byproducts: the baked binaries are derived (rebuild with bake.mjs),
# and the driver's Chrome profile is ~120 MB. The spike's own code and its results
# (JSON + the screenshots the findings cite) are committed as the evidence trail.
spike/render-budget/data/
spike/render-budget/.chrome-profile/

# Local Claude settings
.claude/settings.local.json

# OS / tooling byproducts
.DS_Store
*.log
.env*
