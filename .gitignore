node_modules/
dist/
.astro/
.vercel/
.vercel

# Data: the raw ATUS downloads are fetched by the pipeline, never committed.
# (Ticket 02 pinned their URLs + SHA-256s; ticket 03 re-fetches and re-verifies.)
data/raw/

# Generated at build, never committed.
src/data/
public/data/
public/og/

# Local scratch — never ship
.scratch/

# Local Claude settings
.claude/settings.local.json

# OS / tooling byproducts
.DS_Store
*.log
.env*
